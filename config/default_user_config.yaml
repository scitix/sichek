nvidia:
  name: nvidia
  query_interval: 10
  cache_size: 10
  ignored_checkers:
    - app-clocks
infiniband:
  name: "infiniband"
  query_interval: 10
  cache_size: 10
  ignored_checkers:
    - "net_operstate"